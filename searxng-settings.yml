use_default_settings: true
server:
  secret_key: "randomGeneratedSecretKeyForSearXNG"
  base_url: "http://localhost/search"
  bind_address: "0.0.0.0"
  port: 8080
  image_proxy: true
  limiter: false  # Disable rate limiting for internal API access
general:
  debug: false
search:
  formats:
    - html
    - json
